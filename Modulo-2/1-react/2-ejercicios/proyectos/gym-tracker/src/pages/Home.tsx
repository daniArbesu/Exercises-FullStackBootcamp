import React from 'react';
import UserProfile from '../components/UserProfile';

const Home = (): React.ReactElement => {
  return (
    <div>
      <UserProfile />
    </div>
  );
};

export default Home;
