import React from 'react';
import { useParams } from 'react-router-dom';

const Routine = (): React.ReactElement => {
  const params = useParams();

  return <div>Routine</div>;
};

export default Routine;
